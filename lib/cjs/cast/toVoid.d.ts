/**
 * A function that does nothing and returns nothing (void).
 *
 * This can be used in situations where a function is required but no operation is needed.
 *
 * @returns {void}
 */
export declare const toVoid: () => void;
